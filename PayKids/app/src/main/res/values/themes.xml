<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PayKids" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PayKids" parent="Base.Theme.PayKids" />

    <style name="Theme.Start" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/blue1</item>
<!--        <item name="android:windowBackground">@drawable/gradient_splash</item>-->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>
        <item name="postSplashScreenTheme">@style/Theme.PayKids</item>
    </style>
</resources>